Propgram	|	Function
--------------------------------
prompt		|	Desplays message $1 and executes $2 based on No/yes(only options)
dsiplatselect	|	Choose which display to use. Has some presets and custom configuration
shutdown	|	Shuts down the computer after prompt. TODO: Expand to other locking functions(i3)
